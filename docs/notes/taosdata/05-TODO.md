# 待办事项

## 使用playwright + MCP 自动抢票

- ~~panelCard的文字覆盖重合~~
- ~~sql高级查询，边界值设置问题~~
- ~~TDDialog组件设计~~
- ~~各chart-type添加UoM~~
- ~~图片资源请求跨域问题~~
- ~~bar-gauge，第一次加载数据会有undefined闪动~~
- ~~bar-gauge支持多metric问题~~
- ~~定时报告点击添加会自动收回~~
- ~~分析面板里面放大和缩小不工作~~
- ~~echart默认大一些、鼠标移动到list上面后出现滚动条~~
- ~~min、max测试是否带入配置~~
- ~~dashboard的文字无法移除~~
- ~~barGauge顶部的文字显示逻辑~~
- ~~tooltip支持留存~~
- ~~阈值、值映射、数据连接显示配置了多少条提示~~
- ~~而且DataLink输入URL的时候，不应该强制用户输入http的，不输入，就自动加上http:// , Url，应该全大写 URL~~
- ~~datalink 绑定到tooltip~~
- ~~table的拖拽图片在windows上不显示~~
- ~~table页面bug, 在编辑状态调整了行的排序，但在查看页面不生效~~
- ~~gauge图单位放到数字上~~
- ~~panelCard 的title显示问题~~
- ~~时间范围调整工具遮住了x轴，图列hide的时候，不用显示配置位置和宽度，图例值的配置~~
- ~~trend charts在dashboard抖动问题~~
- ~~修改了基础库的属性（电流 - minimum/maximum）,元素浏览器中的元素属性没有同步修改，并且无法编辑~~
- ~~tooltip销毁提示~~

- ~~坐标轴配置背景色区分x、y~~
- ~~windows 下空panel会产生滚动条~~
- ~~自定义配色如何与指标匹配~~
- ~~鼠标悬浮图表的时间提示处理~~
- ~~字段覆盖，改为自定义配置~~
- ~~直方图的标识线图标问题~~
- ~~直方图e2e的添加~~
- ~~趋势图的状态图需要值映射等逻辑~~
- ~~图例没有加i18n~~
- ~~api生成的平滑折线图，编辑的时候跳到直线了~~
- ~~bargauge:~~
  - ~~show unfilled area模式下value Display不生效~~
  - ~~小数位数修改不生效~~
  - ~~填写min后lcd模式渲染问题，下方文字被覆盖~~

- ~~panel的配置项的初始化位置不正确（panelDefaults.ts），不应该在\*Properties中，而应该在`panel-layout`里面，这样能适配api、mcp等方式生成的面板~~
- ~~heatmap:~~
  - ~~x轴可配置为指标，不一定要时间~~
- ~~直方图/蜡烛图的数据分析按钮在view的状态下也要保留~~
- ~~pie类型当无维度时时间戳要转为标准时间格式~~
- ~~仪表板分组优化~~
  - ~~整个分组的优化，像调整面板位置一样调整分组。一个gridstack~~

- ~~添加分组，定位到最下面~~
- ~~仅删除分组，分组里的面板保留在原来分组的位置~~
- ~~点选group，然后右键group~~
- ~~趋势图-多泳道-图例消失~~
  - ~~多泳道无需图例~~
- ~~仪表板刚进来就提示保存了~~
- ~~dashboard：跳转页面添加panel，返回时添加的位置不对~~

- spc
  - ~~实时更新点~~
  - ~~从事件入口进入、从面板进入...~~
  - ~~添加其他事件不要显示spc分析按钮~~
  - ~~判异规则的弹框可以勾选规则显示隐藏~~
  - ~~判异规则的配置需要存储~~

  -~~ 从实时分析或实时分析保存的面板进入：打开数据刷新按钮 & 查询对应时间段的所有事件 （实时监控）~~
  - ~~保存为面板：只存analyzeId~~
  - ~~从事件入口或事件入口保存的面板进入：关闭数据刷新按钮 & 只加载当前的事件到分析工作台 （不实时监控）~~
    - ~~保存为面板：只存eventId~~

- 优化项
  - panel的echart渲染，在拖动width、height时，触发更新的频率太快了，需要加上节流函数
  - 加载页面，有些 XHR 请求重复了,并且会请求大量js资源（优化请求时机+缓存），导致性能卡顿
  - 项目中的form表单里的每一项都是双行显示，`文字提示单占一行`，视觉上看起来很乱，可考虑单行显示
    - 考虑到中英切换，可开发控件，鼠标悬浮时自动展开文字提示
    - 文字提示（label）可加上背景色
  - 有些页面的空白太多
  - 注册流程分成了两个页面，有些反逻辑，可考虑优化
  - 元素树层次太多，需要点击好几层才找到目标，可添加层级展开功能和搜索功能

- 趋势图：
  - 数据堆叠与多泳道同时开启的时候逻辑有问题
  - 设计：配置某些区域的点及线为固定颜色（自定义配色的颜色悬浮显示系列名）
  - 趋势图添加的边界值的线，线的label显示到canvas外面去了
  - 可以将边界值配置和阈值配置合并 (多泳道时每个泳道可以配置边界线)
  - 重构边界值可以为不同的属性分别配置边界值
  - 趋势图/柱状图，数据每秒刷新的时候，y轴范围会跳动，需要优化处理
  - trend chart需要可以处理非number类型数据
  - 用户友好优化：点的触发区域需要变大
- table
  - table设置弹框有问题:勾选-定宽-排序 三者有冲突
  - 翻页支持设置每页的页数
  - 数字居右，其他居左
- bar
  - bar width会导致重叠
  - 堆叠模式有问题：em1和em2的电压堆叠
  - show values在card中显示有问题
  - y轴的min/max会影响堆叠效果
- statetimeline
  - card模式最好可以把label放平
  - 默认行高设置为0.6
- status-history
  - shade of color,手动改颜色后就不生效了
  - 默认行高设置为0.6
- scatter
  - special points 失效
  - limit失效
- candlestick
  - legend有问题
  - 多个设备的相同电压全都显示了
- gauge
  - 多个指标生成表达式不对
  - x轴的时间格式配置只在时间序列数据中显示
- histogram
  - traits数据是绑定在yaattribute中的，没有更新

- panel
  - 面板模板创建时又出现重复多个面板的情况了
  - 多泳道模式下如果很多泳道，y轴的标题可能覆盖上一个泳道
  - 面板模版，编辑页面切换元素的时候，还保留着上一个y轴名称的配置
  - panel的列表list没有显示分类信息、并且分类过滤不生效
  - ~~非叶子节点，关闭包含子元素时，查询报错~~

- dashboard
  - panel隐藏边框后，鼠标悬浮需要加一个panel的悬浮效果

- 组态
  - 现在进度的展示只能是 0 - 1 ，如果对应的参数是 0-100 该怎么设置？比如阀门开度，都是 65 这种表示 65%
  - ~~鼠标移入图元，在编辑状态图元背景色没有显示~~
  - 从图源库拖动图元到画布快速松开，添加失败
  - ~~图标边框颜色~~
  - 单位在定时刷新后会消失
  - 检查组态列表，不可以转换为模版，把这个选项去掉
  - 组态似乎绘制了两次，会有闪动
  - ~~组态用户手册更新~~
  - 右键菜单添加到分析工作台
    - 在同一个弹框中显示多个属性趋势图
  - 弹框数据实时刷新
  - 希望可以看到更多信息

- 元素属性
  - ~~基础库，修改元素属性的值类型的时候（int -> enum）,数据引用被清空了。请排查什么时候应该清空、保留、恢复~~

- spc
  - 点出界：
    - 点回归到正常区域后，事件没有结束
  - 连续5点中4点在1sigma外（同侧）：
    - 同一个点有可能生成了两个事件，是否应该保证同种判异规则类型的事件在时间窗口内不要重叠


- orbstack