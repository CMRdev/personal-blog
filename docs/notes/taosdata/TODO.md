# 待办事项

- ~~panelCard的文字覆盖重合~~
- ~~sql高级查询，边界值设置问题~~
- ~~TDDialog组件设计~~
- ~~各chart-type添加UoM~~
- ~~图片资源请求跨域问题~~

- panel的echart渲染，在拖动width、height时，触发更新的频率太快了，需要加上节流函数
- 首次加载页面，有些 XHR 请求重复了,并且会请求大量js资源，导致性能卡顿
- history-status未处理非时间序列的数据
- history-status的rect上的文字重合了【`暂时不处理-此种类型只支持叶子结点`】
  - 原因：接口返回的数据有相同时间点的重复数据
- bar-gauge，第一次加载数据会有undefined闪动
- bar-gauge支持多metric问题
- 首次加载请求的资源太多了（未验证打包发版时是否对资源进行了打包），导致页面卡顿严重，可以思考如何优化


- 面板的AI提示生成的内容占据了太大空间了，可以考虑分屏横向布局，用户体验可能会有提高
- panels的各种echarts图表，代码耦合比较严重，可适当拆分，方便维护
- 项目中的form表单里的每一项都是双行显示，`文字提示单占一行`，视觉上看起来很乱，可考虑单行显示
    - 考虑到中英切换，可开发控件，鼠标悬浮时自动展开文字提示
    - 文字提示（label）可加上背景色
- 有些页面的空白太多
- 注册流程分成了两个页面
- 元素树层次太多，需要点击好几层才找到目标，可添加层级展开功能和搜索功能