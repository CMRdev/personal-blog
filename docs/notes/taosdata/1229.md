# 环境搭建

##

- 被拉取进 github 项目时，需要 2FA 认证，在手机上下载 TOTP 软件（应该是一个安全策略，后续了解）

- 本机安装`homebrew`、`git配置`

```bash
MingRen Chen
MingRen Chen@taosdata.com
```

- `提交一个PR`指的是创建一个 ‌Pull Request（PR），‌PR 的核心作用是促进代码审查和团队协作

- `Jira`缺陷跟踪管理系统，可创建`issue link`

- 安装 node+npm （nvm）

```bash
# Download and install nvm:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash

# in lieu of restarting the shell
\. "$HOME/.nvm/nvm.sh"

# Download and install Node.js:
nvm install 24

# Verify the Node.js version:
node -v # Should print "v24.12.0".

# Verify npm version:
npm -v # Should print "11.6.2".
```

- 安装 python `https://www.python.org/downloads/`

- 安装 JDK、Maven

- 安装 IDEA

- nvm 命令失效

```bash
编辑：~/.zshrc
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

刷新配置：source ~/.zshrc
```

- docker 启动镜像 engine

```bash
docker pull tdengine/tdengine:latest
# docker pull tdengine/tdengine:3.3.0.0
docker run -d -p 6030:6030 -p 6041:6041 -p 6043-6060:6043-6060 -p 6043-6060:6043-6060/udp tdengine/tdengine
# presist data
docker run -d -v <local-data-directory>:/var/lib/taos -v <local-log-directory>:/var/log/taos -p 6030:6030 -p 6041:6041 -p 6043-6060:6043-6060 -p 6043-6060:6043-6060/udp tdengine/tdengine
```

- 配置 maven

```bash
# 找到java的安装目录
/usr/libexec/java_home -V
# 写入环境变量
echo 'export JAVA_HOME=$(/usr/libexec/java_home)' >> ~/.zshrc
echo 'export PATH=$JAVA_HOME/bin:$PATH' >> ~/.zshrc
source ~/.zshrc
```
