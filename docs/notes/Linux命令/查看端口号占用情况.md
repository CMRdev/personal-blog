# lsof

- `lsof -i :6039`

- `lsof -iTCP -sTCP:LISTEN -n -P`

```bash
lsof
# 表示 list open files。在 macOS / Linux 里，网络连接、端口、文件都可以看作“打开的文件”，所以 lsof 也能查端口占用。
-iTCP
# 只查看 TCP 网络连接。
-sTCP:LISTEN
# 只显示 TCP 状态为 LISTEN 的连接，也就是“正在监听端口”的进程。
-n
# 不把 IP 地址解析成域名。
-P
# 不把端口号解析成服务名。
```

- `kill -9 PID`
